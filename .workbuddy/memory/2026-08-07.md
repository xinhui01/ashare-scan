# 2026-08-07 工作日志

## 超短预测策略优化分析（代码实测）
- 读取 `src/services/scoring/{predict,quality_gate,wrap,trend,fresh_calibration}.py`、`opening_confirmation_service.py`、`stock_filter.py`、`scan_models.py`、快照。
- 关键结论（代码事实）：
  1. 质量门已默认启用：`predict.py:2428` 调 `wrap_first_cont`（0805方案第1条已落地）。
  2. **命中率标定 `calibrated_score` 只覆盖 fresh 类**（`calibrate_fresh_candidate` 仅在 `fresh.py:101` 调用）；反包/二波/保留三类无标定 → 竞价确认 `score>=60` 用错分。
  3. 分数=形态完整度非涨停概率；质量门类内排序仍按原始 `score`（`quality_gate.py:125`），反包高分常是连板耗尽股（传智8连板+30日+111%仍100分）。
  4. 反包板块 bonus 用 raw industry（证监会粗命名），与 hot_industries（东财窄命名）对不上→信号大概率死（trend.py:406 同 bug 已修、wrap 未修）。
  5. 无 ML 模型（scan_models.py 仅数据类）；无出场/止损模块（退潮/破5日线全靠手动纪律）。
  6. 候选有 `score`(0-100) 与 `final_rank_score`(可>100) 两套分，文档"128分"是未截断 rank 分，跨类比无意义。
- 产出：`策略/优化分析_预测策略优化点_0807.md`（按 P0-P3 排，含代码证据+下周可执行清单）。
- 最高 ROI 三项：①反包/二波/保留也标定命中率；②反包板块判断改 em_industry_map + 要求同板块涨停共振；③新增 exit_signal_service 把破5日线/未涨停当天走写进代码。
