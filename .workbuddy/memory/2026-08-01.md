# 2026-08-01 工作日志

## 资金流 ProxyError 后续
- 用户确认：**不需要改代理相关代码**（"不需要做代理"）。资金流全挂根因维持昨日结论——本机 env 里 `HTTPS_PROXY=http://127.0.0.1:7897` 指向假的 Clash 端口，联网请求全 ProxyError；历史K线走本地缓存不受影响。
- 处置方式：环境问题，靠用户侧清理/修复 Clash 代理解决，不动 `_set_proxy.bat`、不动资金流网络路径。
- 后续若再遇同类 ProxyError，优先让用户检查 Clash 7897 是否在真正提供 HTTP 代理，而非改代码。

## 资金流代理实测（用户要求跑测试）
- 用项目 `.venv`(akshare 1.18.63) 对 600036 写脚本 `_test_fundflow_proxy.py` 测两场景：
  - 直连(清 env 代理)：沙箱内 `ConnectionError: RemoteDisconnected`（沙箱无外网出口，非本机情况）。
  - 强制 `HTTPS_PROXY=127.0.0.1:7897`：**精确复现用户日志的 `ProxyError(...RemoteDisconnected)`**。
- 结论：ProxyError 被坐实是 env 代理指向死 7897 导致，非代码/东财问题；沙箱无 egress 故无法在此跑出"拉数成功"。需用户本机跑脚本确认直连成功。
- 临时脚本 `_test_fundflow_proxy.py` 留在项目根，待用户决定保留或删除。

## 已执行：清理走代理的代码（用户选"全部直连+删代理池"）
- 删 `src/network/proxy_pool.py`（免费代理池）及其引用。
- `stock_data.py`: `need_no_proxy` 改为始终 `True`（强制所有 Session `trust_env=False` 忽略 env 代理）；删掉代理池 import 别名块与启动线程。
- `src/sources/eastmoney/session.py`: 删 `get_proxy`/`blacklist_proxy` 引用与代理分支，`get_json` 固定直连。
- `_set_proxy.bat`: 改为只清空 HTTP(S)_PROXY、走直连（不再注入 Clash 7897）。
- `main.py`/`README.md` 注释同步；删掉我之前诊断用的 `_test_fundflow_proxy.py`。
- 校验：无残留 proxy 引用；`stock_data`/`session`/`main` 编译通过；运行时验证所有 Session `trust_env=False`，东财 session 无代理逻辑。
- 写入 `.workbuddy/memory/MEMORY.md` 项目约定：网络全程直连，遇 ProxyError 查本机 env 而非改代码。

## 资金流启动探针 + 同花顺兜底防漏股（用户要求：启动时检查、优先修 Clash 根、ths 按 code 翻页）
- 背景：用户关了 Clash 的 7897 HTTP 代理但仍报 `Connection aborted. RemoteDisconnected`（直连被 reset）。
  根因仍是**本机 Clash 的 TUN/增强模式在网络层劫持 eastmoney 流量并 reset**，与 7897 HTTP 代理无关。
- 实测确认：本机/沙箱 东财 `push2his` 被 reset，但同花顺 `data.10jqka.com.cn` 通；腾讯/新浪资金流接口已失效。
  同花顺榜单仅含 流入/流出/净额（无主力/大单拆分）；akshare `stock_fund_flow_individual` 用 zdf 翻页会漏掉 000938。
- 改动：
  1. `stock_data.py` 新增 `check_fund_flow_connectivity()`（模块导入时后台线程触发，幂等，探针在锁内执行避免竞态）：
     探测东财/同花顺直连；东财不可达→主动熔断东财+打印 Clash TUN 处置提示；状态存 `_FF_EM_REACHABLE/_FF_THS_REACHABLE`。
  2. `build_fund_flow_request_plan(auto)`：东财不可达或熔断时 `("ths","eastmoney")`，首轮即走同花顺。
  3. 新增 `StockDataFetcher._eastmoney_fund_flow_expected()`；`get_fund_flow_data` 缓存块：东财不可用且缺大单时不空转刷新，当天有缓存即接受。
  4. `src/sources/ths.py` 重写 `fetch_fund_flow_frame`：按 code 降序**二分翻页**定位个股（修复漏股），
     `_ths_page_frame` 加重试+hexin-v 失效刷新（应对偶发 401），邻页窗口 ±2，兜底失败清空 vcode 重试。
- 校验：5 只跨范围股票(000938/600036/300750/000001/688981)均稳定命中；`get_fund_flow_data` 东财不可达时走同花顺返回 1 行不报错不空转；
  探针标志位正确置位（无竞态）；401 重试生效；编译通过。临时诊断脚本 `_diag_net.py` 已删。
- 清理：测试时写入 000938 的净额兜底下记录已从 `data/stock_store.sqlite3` 的 fund_flow 表删掉，避免污染真实缓存。

## 用户要求实跑验证（23:01）
- 写 `_verify_fundflow.py` 端到端验证：①探针 `_FF_EM_REACHABLE=False/_FF_THS_REACHABLE=True` 且打印 Clash TUN 提示；
  ②同花顺二分翻页跨 6 只(000938/600036/300750/000001/688981/002594)全部命中；③`get_fund_flow_data('000938')` 东财不可达走同花顺返回1行无报错(0.4s)；
  ④二次调用不空转；⑤eastmoney/ths 显式模式 intact。编译通过，测试缓存已清理。
- 注意：沙箱网络受限只能验证"兜底降级"路径；东财完整主力/大单路径需用户本机(东财通)跑 `_verify_fundflow.py` 确认。兜底时 main_force/big_order 均=净额(同花顺无拆分)，为近似。

## 用户本机实测日志确认（23:05）
- 本机跑应用：直连模式生效（bat 输出"已清空 HTTP(S)_PROXY，所有请求走本地直连"，GBK 终端呈乱码但功能正常）；
  但东财仍被连续失败触发熔断器（冷却30s），提示 Clash TUN/增强模式仍在接管 eastmoney 流量并 reset。
- 证实：仅清掉 7897 HTTP 代理不够，Clash 的 TUN/增强模式会在网络层继续拦 eastmoney；熔断器+同花顺兜底按设计生效，
  未再弹"所有数据源失败"报错窗（降级成功）。
- 处置：用户需彻底退出 Clash 或关闭其 TUN/增强模式，让东财直连通，方可恢复完整主力/大单数据。
